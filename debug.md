# 動作確認メモ

ヘッドレス Chrome (152) を CDP で操作して `http://localhost:3000/` と `/ru/` を一通り触った結果。
**修正はしていない。** 見つかった順ではなく、影響の大きい順に並べた。

確認して問題がなかったものは末尾にまとめてある。

---

## 1. 「すべて外す」「ピンを外す」が取り消せない

**重要度: 中（データを失う操作が不可逆）** — ✅ **修正済み**
（`syncUrl(push)` を足し、ピンを外す2つの操作でだけ `pushState`。
戻る/進むは `popstate` → `location.reload()` で読み直す。他の操作は今まで通り `replaceState` なので履歴は積まれない）

`drill.js:594` に

```js
pins = []; // 戻したいときはブラウザの戻るで直前の ?pins= に戻れる
```

とあるが、**戻れない。** `syncUrl()` が `history.replaceState`（`drill.js:357`）なので、
`?pins=` を持つ履歴エントリはその場で上書きされ、履歴に残らない。

### 再現手順

1. `http://localhost:3000/?v=parler&pins=parler%7C%7C%7C%7C%7C,aller%7C%7C%7C%7C%7C&q=1` を開く
2. 「すべて外す」を押す → URL が `?v=parler` になる
3. ブラウザの戻る

### 起きること

直前の `?pins=…` には戻らず、そのタブでその前に見ていたページに飛ぶ。
`exercices.html` から `target="_blank"` で開いた新規タブなら履歴が1件しかないので、**戻り先が存在しない = 問題集が完全に消える。**

個別の ✕（`.pin-del`）でも同じ。押した瞬間 `syncUrl()` が走るので取り消せない。

---

## 2. 出題モードを画面上で ON にすると、「消した人称を表示」の ✕ が残る

**重要度: 中（押すと問題集の外に飛ぶ）** — ✅ **修正済み**
（`#quiz-mode` のトグルで `build()` を呼ぶようにした。ON/OFF どちらの向きでも ✕ が追従する）

`renderState()`（`drill.js:175`）は `quizMode` を見て ✕ を出し分けているが、
`renderState()` は `build()` からしか呼ばれない。`#quiz-mode` のトグルは `renderPins()` と `syncUrl()` しか呼ばないので、**表示が更新されない。**

### 再現手順

1. `http://localhost:3000/?v=parler&p=012` を開く（je・tu・il を消した表）
2. 表の上に「人称 3/6 ✕」のチップが出ている
3. ピン留めボタン（🔖）を押す
4. 「問題集（ピン留め）から出題」を押す → 出題モード ON
5. **チップの ✕ がまだ残っている**（URL から `q=1` で入ったときは正しく消えている）
6. その ✕ を押す

### 起きること

- 消していた人称が全部戻り、表が問題集の1件と一致しなくなる
- ピン留めボタンが押されていない状態に変わる（`aria-pressed="false"`）
- でも出題モードは ON のまま、動詞ドロップダウンは無効のまま
- → **出題中なのに「問題集の外の表」を解いている状態**

逆向きも同じ。出題モードを OFF に戻しても ✕ は戻ってこない（次に `build()` が走るまで）。

```
出題ON(URL経由) chip-x: 0
出題OFF にした直後 chip-x (1 が期待値): 0
採点→やり直しのあと chip-x: 1
```

---

## 3. 出題モード中に行・列を消すと、画面上に戻す手段がない

**重要度: 中** — ✅ **修正済み**
（出題中は消去ボタン自体を出さないことにした。戻す ✕ を出さないのと対称）

### 再現手順

1. `http://localhost:3000/?v=parler&pins=parler%7C%7C%7C%7C%7C,aller%7C%7C%7C%7C%7C&q=1` を開く
2. 「答え合わせ」を押す（消去ボタン ✕ が出る）
3. 表の一番下に出た列の ✕ を1つ押す

### 起きること

- URL に `&t=0` が付く
- ピン留めボタンが未押下になる（問題集の外の表になった）
- 3 の設計通り、出題モードなので「消した時制を表示」の ✕ は **出ない**
- リロードしても `t=0` は URL に残るので、消した状態が復元される

つまり **「次の動詞」を押すまで元に戻せない。** 消す操作は出題モード中でもできるのに、戻す操作だけが封じられている。
（「次の動詞」を押せば `applyPin()` で復帰はする）

---

## 4. 同じ表が2件ある問題集 URL で、ピンを外しても押下状態が残る

**重要度: 小**

### 再現手順

1. `http://localhost:3000/?pins=parler%7C%7C%7C%7C%7C,parler%7C%7C%7C%7C%7C&q=1` を開く
2. 一覧に `parler` のチップが2つ並ぶ
3. ピン留めボタン（🔖）を押して外す

### 起きること

`$("pin")` のハンドラは `findIndex` で最初の1件しか消さないので、
1回押しても `aria-pressed` が `true` のまま（残った同一表にまだ一致するため）。
「外したのに外れていない」ように見える。

画面操作だけでは同一ピンは作れないが、`exercices.html` や `quiz-url` スキルが吐く URL では起こりうる。

---

## 5. ロシア語版で「1マスも書けない表」が作れる

**重要度: 小**

### 再現手順

`http://localhost:3000/ru/?v=прочитать&t=1234` を開く（完了体 + 現在だけ残す）

### 起きること

- 6行 × 1列、全マスが `—`（完了体に現在はない）で `disabled`
- `focusStart()` が書けるマスを1つも見つけられず、フォーカスは `<body>` のまま
- それでも「答え合わせ」は押せて、押すと即「やり直す」に変わり、用例まで表示される
- 行の消去ボタンは6個出る（1マスも解いていないのに）

同じことは `быть`（現在なし）でも起きる。
`?t=` を手で書くか、`быть` の表で「現在」以外の列を全部消せば到達する。

---

## 6. `exercices.html` の件数表記が中身と合わない

**重要度: 小（表示のみ）**

```
11件 — 人称ごとに全時制 (5)、時制ごとに全人称 (5) ※複合過去・大過去は除く
```

実際の `?pins=` の中身は **人称ごと 6件 + 時制ごと 5件 = 11件**。
`(5)` が「件数」なら人称側は 6 が正しい。「時制の本数」の意味なら時制側の `(5)` は人称数 6 と合わない。
どちらの読みでも1つ合わない。

再現: `http://localhost:3000/exercices.html` を開いてリンクの `pins` パラメータをカンマで数える。

---

## 7. フランス語版からロシア語版への導線がない

**重要度: 小**

- `/ru/` のフッターには「フランス語版へ」（`href="/"`）がある
- `/` のフッターには GitHub の issue リンクしかない

```
fr のリンク: [ 'https://github.com/kamataryo/conjugaison-seyes/issues/new' ]
ru のリンク: [ '/', 'https://github.com/…/issues/new' ]
```

片方向だけの導線なので、`/` から入った人はロシア語版の存在に気づけない。
`exercices.html` もフランス語版専用で、ロシア語版に対応するページがない。

---

## 8. 複合過去・大過去を消した表でも、助動詞が伏せられたまま

**重要度: 小**

`app.js` のコメント通り「助動詞は複合過去・大過去の答えそのものなので伏せる」のは妥当だが、
**その2つの時制を表から消しても伏せられたまま。**

### 再現手順

`http://localhost:3000/?v=aller&t=56` を開く（複合過去・大過去を消す）

### 起きること

列は「直説法現在／半過去／単純未来／条件法現在／接続法現在」の5本だけなのに、
見出し行は `aux. [?]` のままで、押さないと `être` が見えない。
この表では助動詞はどのマスの答えでもないので、伏せる理由がない。

`exercices.html` の問題集2つはどちらも複合過去・大過去を除外しているので、常にこの状態になる。

---

## 9. 誤答集計だけが先頭の代名詞を落とすので、正解の綴りが「誤答」として残る

**重要度: 小（現在は画面に出ないが記録は溜まる）**

前提として、マスに書くのは活用形だけ（`parle`）で、正解表示の `je ` は
`.ans > .pr` として弱い色・非太字で添えてあるだけ ― これは仕様通り。
問題は、その仕様と `normalizeWrong` の畳み方が噛み合っていないこと。

### 再現手順

1. localStorage を空にする
2. `http://localhost:3000/?v=parler&p=12345&t=123456` を開く（je × 直説法現在 の1マス）
3. `parles` と入力して「答え合わせ」→「やり直す」
4. うっかり `Je Parle ` と代名詞つきで入力して「答え合わせ」
5. DevTools で `localStorage["conjugaison.stats"]` を見る

### 起きること

```json
{"parler|je|present":{"n":2,"ok":0,"wrong":{"parles":1,"parle":1}}}
```

`conjugate.js` の `normalizeWrong` は先頭の人称代名詞を落とす（`je ` / `j'` / `tu ` …）。
誤答が「代名詞を書いたこと」だけだった場合、畳んだ結果が正解の綴りと同じになるので、
**`parle` が「よくある誤答」として記録される**。何を間違えたのかが消えてしまう。

（綴りを間違えた誤答を `je parles` と `parles` で二重に数えない、という
`normalizeWrong` 本来の狙いは正しく効いている。困るのは代名詞だけが誤りのときの1ケース）

`renderStats()` は今のリリースでは呼ばれていない（`drill.js:232` でコメントアウト）ので
画面には出ないが、記録は溜まり続けるので、表示を戻した時点で誤答一覧に正解が並ぶ。

ロシア語版も同じ（`я читаю` → `читаю` として記録）。

---

## 10. `мочь` だけ未来が全マス `—`

**重要度: 情報（データの意図確認）**

`ru/verbs.json` の `мочь` は `forms.future: null` なので、未来の列が6マスとも `—` になる。
不完了体なので本来は `буду мочь` が機械的には作れる。

一方 `хотеть` は `forms` に `future` がないため `буду хотеть` が正解として出る。
`буду мочь` も `буду хотеть` も実際にはほとんど使わない表現なので、
**片方だけ `null` にしてあるのが非一貫**に見える。意図的なら問題なし。

再現: `http://localhost:3000/ru/?v=мочь` と `http://localhost:3000/ru/?v=хотеть` を並べて見る。

---

## 確認して問題がなかったところ

- **採点の対象は活用形だけ**: マスに書くのは `parle`、正解表示の `je ` は `.ans > .pr`（weight 400 + `--ink-soft`）で
  添えてあるだけ。`je parle` と書けば不正解になるが、これは仕様通りで表示とも矛盾しない
- **活用の生成**: fr 73動詞 × 7時制 × 6人称 × 男女、ru 54動詞 × 5時制 × 6人称 × 男女 を総当りで生成。例外・`undefined`・空文字・余分な空白すべてゼロ
- **女性形の別解**: `?v=aller` の複合過去に `suis allée` / `suis allé` どちらも正解になり、正解表示の性も入力に合う。`avoir` を取る `parler` に `ai parlée` は正しく不正解
- **ロシア語の伏せマス**: 完了体の現在、命令法の я/он/мы/они、`быть` の現在、`мочь`/`хотеть` の命令法 — すべて `—` で `disabled`、採点対象外
- **-ся 動詞**: `заниматься` の現在・過去・未来（`буду заниматься` は助動詞が非再帰）・仮定法・命令法すべて正しい
- **壊れた URL**: `?v=zzzz`（存在しない動詞）、`?p=999`（範囲外）、`?p=012345`（全消し）、`?op=999999`（不正な並び）、`?pins=zzz|…`（存在しない動詞のピン）— すべて安全に既定値へ落ちて URL も書き直される。コンソールエラーなし
- **最後の1本は消せない**: `?p=01234&t=012345` で 1×1 の表になり、消去ボタンは出ず、シャッフルも `disabled`
- **シャッフルの URL 往復**: `op=531204&ot=1065234` をリロードして同じ並びが復元される
- **転置**: 見出しの字面（人称=セリフ体、時制=小さな大文字）が入れ替えても保たれ、消去の軸も正しく対応する
- **出題モードの巡回**: 2件の問題集で「次の動詞」を6回押して `aller → parler → aller → …` と交互に出る（同じ表を連続で引かない）
- **Enter キー移動**: 実キー入力では列を下に進み、空セルを直前の答えで埋め、最下段で次の列の先頭へ折り返す。右下で止まる
- **横スクロールのヒント**: 390×780 (mobile) で表がはみ出すと「横にスクロールできます」が出る
- **統計の集計単位**: `動詞|人称|時制` で正しく、転置しても人称と時制が入れ替わらない。空欄と `—` のマスは記録されない
- **コンソール**: 全操作を通じてエラー・例外・警告ゼロ

---

## 検証環境

- ヘッドレス Chrome 152.0.7977.76 を `--remote-debugging-port` で起動し、CDP を直接叩いて操作
- 合成 `KeyboardEvent` では2回目以降の Enter が効かないように見えるが、これは
  ヘッドレスでページがフォーカスを持たず `focus` イベントが発火しないためのテスト側の都合。
  `Input.dispatchKeyEvent`（実キー相当）では正常に動く。**アプリのバグではない**
